[Test]
  issues = '#23470'
  design = 'UserObjects/index.md'

  [simultaneous]
    type = CSVDiff
    input = test.i
    cli_args = 'Outputs/file_base=simultaneous'
    csvdiff = simultaneous.csv
    requirement = "The system shall execute user objects in the same group in the order given in the design documentation."
  []

  [two_groups]
    type = CSVDiff
    input = test.i
    cli_args = 'Outputs/file_base=two_groups Postprocessors/pp3/execution_order_group=3'
    csvdiff = two_groups.csv
    requirement = "The system shall permit adding one new execution order group."
  []

  [three_groups]
    type = CSVDiff
    input = test.i
    cli_args = 'Outputs/file_base=three_groups Postprocessors/pp2/execution_order_group=2 Postprocessors/pp3/execution_order_group=3'
    csvdiff = three_groups.csv
    requirement = "The system shall permit adding multiple new execution order groups."
  []

  [negative_group]
    type = CSVDiff
    input = test.i
    cli_args = 'Outputs/file_base=negative_group Postprocessors/pp1/execution_order_group=-1'
    csvdiff = negative_group.csv
    requirement = "The system shall permit adding a new execution order group with a negative priority."
  []
[]
